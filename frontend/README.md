# Frontend

React + Vite + Tailwind UI for the Ledger portfolio tool.
See the root `../README.md` for full setup instructions.

Quick start: `npm install && npm run dev` (with the backend running on :8000).
